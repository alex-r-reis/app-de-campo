{
  "name": "ater-cacau-campo-server",
  "private": true,
  "version": "1.0.0",
  "description": "Backend que recebe os relatórios do app de campo, envia o .docx e as fotos para uma pasta no Google Drive e registra cada visita em uma linha do Google Sheets. Também serve os dados para o painel de administração.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "googleapis": "^140.0.0",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
